[project]
name = "ecmwflive-data"
version = "0.1.0"
description = "Live ECMWF data pipeline assets."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "xarray",
    "pandera",
    "s3fs",
    "schemas",
    "cfgrib",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ecmwflive_data"]
